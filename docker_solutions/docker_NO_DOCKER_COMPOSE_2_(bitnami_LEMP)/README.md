Docker application with Bitnami
===============================

- This is a Docker application which uses as a base image
  the Bitnami's LEMP stack.
- This Docker application requires only Docker engine (NO_DOCKER_COMPOSE, 
  NO_SWARM)
  